# امتیازی - متد ماک

مطابق توضیحات گفته شده در کلاس (هفته دوم روز پنج‌شنبه، سکشن سوم، مرتبط با یونیت‌تست (دقیقه ۱:۲۵:۳۰ از این فیلم))،  
متد ماک در پایتون را با استفاده از `getattr` و `setattr` پیاده‌سازی کنید.
